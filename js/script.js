document.addEventListener("DOMContentLoaded",()=>{const current=(location.pathname.split("/").pop()||"index.html");document.querySelectorAll("nav a").forEach(a=>{if(a.getAttribute("href")===current)a.classList.add("active")});const show=(id,text,ok=true)=>{const el=document.getElementById(id);if(el){el.textContent=text;el.style.color=ok?"#355e3b":"#b00020"}};const comment=document.getElementById("commentForm");if(comment)comment.addEventListener("submit",e=>{e.preventDefault();const name=document.getElementById("commentName").value.trim();const text=document.getElementById("comment").value.trim();if(!name||!text)return show("commentMessage","Please fill out both fields.",false);show("commentMessage",`Thank you, ${name}! Your comment has been submitted.`);comment.reset()});const signup=document.getElementById("signupForm");if(signup)signup.addEventListener("submit",e=>{e.preventDefault();const name=document.getElementById("fullname").value.trim();const email=document.getElementById("email").value.trim();const genre=document.getElementById("genre").value;const bio=document.getElementById("bio").value.trim();if(!name||!email||!genre||!bio)return show("signupMessage","Please complete all required fields.",false);show("signupMessage",`Welcome to The Forest of Stories, ${name}!`);signup.reset()});const contact=document.getElementById("contactForm");if(contact)contact.addEventListener("submit",e=>{e.preventDefault();const name=document.getElementById("contactName").value.trim();const email=document.getElementById("contactEmail").value.trim();const subject=document.getElementById("subject").value.trim();const text=document.getElementById("contactText").value.trim();if(!name||!email||!subject||!text)return show("contactMessage","Please complete all fields.",false);show("contactMessage",`Thank you, ${name}! Your message has been sent.`);contact.reset()})});